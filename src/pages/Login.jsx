import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { useApp } from '../context/AppContext'
export default function Login() { const [email, setEmail] = useState('customer@parksmart.com'); const [password, setPassword] = useState('1234'); const [error, setError] = useState(''); const { login } = useApp(); const navigate = useNavigate(); const submit = e => { e.preventDefault(); const result = login(email, password); if (!result) return setError('That email and password do not match our demo accounts.'); navigate(result.user.role === 'admin' ? '/admin/dashboard' : '/dashboard') }; return <div className="auth-page"><div className="auth-brand"><Link to="/" className="brand"><span className="brand-mark">P</span><span>Park<span>Smart</span></span></Link></div><div className="auth-panel"><div className="auth-copy"><span className="eyebrow">WELCOME BACK</span><h1>Make room<br /><em>for what matters.</em></h1><p>Manage your parking, vehicles and service appointments from one simple dashboard.</p><div className="auth-stat"><strong>24</strong><span>parking slots<br />ready to reserve</span></div></div><form className="auth-form" onSubmit={submit}><span className="eyebrow">SIGN IN</span><h2>Welcome back.</h2><p className="muted">Use one of the demo accounts to explore ParkSmart.</p><label className="field"><span>Email address</span><input type="email" value={email} onChange={e => setEmail(e.target.value)} required /></label><label className="field"><span>Password</span><input type="password" value={password} onChange={e => setPassword(e.target.value)} required /></label>{error && <div className="error">{error}</div>}<button className="button coral full">Sign in <span>↗</span></button><div className="demo-accounts"><button type="button" onClick={() => { setEmail('customer@parksmart.com'); setPassword('1234') }}>Customer demo</button><button type="button" onClick={() => { setEmail('admin@parksmart.com'); setPassword('1234') }}>Admin demo</button></div><p className="form-foot">New to ParkSmart? <Link to="/register">Create an account</Link> · <Link to="/about">About ParkSmart</Link></p></form></div></div> }
